-- WAVE 2 (task "progress-core") WILL REPLACE THIS.
--
-- Placeholder pre-declared by wave 1 ("size-split-and-format") purely so
-- the tree keeps compiling with this module's final cabal shape already
-- in place -- see that task's cabal-file deliverable (6) and final
-- report. Not a real implementation: wave 2 owns site/src/SXC1/Progress/
-- and replaces this file wholesale.
module SXC1.Progress.Scheduler () where
