-- WAVE 3 (task "storage-sink") WILL REPLACE THIS.
--
-- Placeholder pre-declared by wave 1 ("size-split-and-format") purely so
-- exe:app keeps compiling with this module's final cabal shape already
-- in place -- see that task's cabal-file deliverable (6) and final
-- report. Not a real implementation: wave 3 owns site/app/Progress/ and
-- replaces this file wholesale. This is the ONE module M3 permits to
-- import Miso.Storage (see the M3 milestone-verify guard).
module Progress.Store () where
