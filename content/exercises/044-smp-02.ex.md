# Into Sampling mode
ja: # サンプリングモードへ

deck: smp-02
chapter: Part: Sampling
tier: intro
requires: pad-01
summary: Prepare the unit for microphone sampling and learn what Sampling mode shows you.
ja: summary: 内蔵マイクでサンプリングする準備を整え、サンプリングモードの表示の読み方を学ぶ。
cite: guide-book 29 "Press the `REC` button to enter Sampling mode"

Set the unit up for microphone sampling, then step into Sampling mode and learn to read
its display: the flashing lights, the trigger setting, and the level meters.
ja: 内蔵マイクでサンプリングできるように本機を準備し、サンプリングモードに入って表示の読み方を学びましょう。ポイントは、点滅する `REC` ボタンとパッド、トリガー設定、レベルメーターです。

## Prepare for mic sampling
ja: ## マイクサンプリングの準備

type: drill
id: d-3-01
cite: guide-book 28 "select a bank that has pads with no recorded sound"
tags: sampling, intro

Get this unit ready to sample your voice with the built-in microphone.
ja: 内蔵マイクで自分の声をサンプリングできるように、本機を準備しましょう。

### Step

cite: guide-book 28 "select a bank that has pads with no recorded sound"
check: The display reads `SELECT BANK 15` and all 16 pads are unlit.
ja: check: 表示部に `SELECT BANK 15` と表示され、16個のパッドがすべて消灯している。

In Performance mode, use the directional buttons to bring up BANK 15 -- a bank whose
pads have no samples assigned.
ja: 演奏モードで、十字ボタンを使って BANK 15 を選択します。パッドにサンプルが1つも登録されていないバンクです。

### Step

cite: guide-book 28 "Use the `INPUT SELECT` switch to select"
check: The `INPUT SELECT` switch sits at `MIC`.
ja: check: `INPUT SELECT` スイッチが `MIC` の位置にある。

On the side of the unit, set the `INPUT SELECT` switch to `MIC`, the built-in
microphone.
ja: 本機側面の `INPUT SELECT` スイッチで `MIC`（内蔵マイク）を選びます。

### Step

cite: guide-book 28 "Set INPUT VOL to maximum"
check: The `INPUT VOL` slider rests at its top position.
ja: check: `INPUT VOL` スライダーが一番上の位置にある。

Slide `INPUT VOL` all the way up to maximum.
ja: `INPUT VOL` を一番上まで上げて最大にしておきます。

## Find the other bank with empty pads
ja: ## 空のパッドがあるもう1つのバンクを探す

type: lookup
find: guide-book 28 "In BANK 14, you can sample to the pads that are unlit"
id: l-3-01
limit: 45
tags: sampling, intro

BANK 15 starts out with every pad empty, but one other bank also has room for your
samples. Find the note that names that bank and tells you how to spot its empty pads.
ja: BANK 15 は最初からすべてのパッドが空ですが、ほかにもう1つ、サンプルを入れる余地のあるバンクがあります。そのバンクの名前と、空のパッドの見分け方を教えてくれる注記を探してください。

### Hint

Look at the note under step 1 of "Getting ready to start sampling", right beside the
`SELECT BANK 15` screenshot.
ja: 「サンプリングを始める準備」の手順1の下、`SELECT BANK 15` の画面写真のすぐ横にある注記を見てみましょう。

## Which button enters Sampling mode
ja: ## サンプリングモードに入るボタンはどれ

type: quiz
id: q-3-13
cite: guide-book 29 "Press the `REC` button to enter Sampling mode"
tags: sampling, intro

From Performance mode, which button do you press to enter Sampling mode?
ja: 演奏モードからサンプリングモードに入るには、どのボタンを押しますか。

- [x] `REC`
ja: - [x] `REC`
- [ ] `EDIT`
ja: - [ ] `EDIT`
- [ ] `DEL`
ja: - [ ] `DEL`
- [ ] `▶/■`
ja: - [ ] `▶/■`

### Why

Each function button opens its own mode from Performance mode: `REC` enters Sampling
mode, `DEL` enters the deletion mode, and `EDIT` enters the editing mode -- `REC` is
the one that records.
ja: 各機能ボタンは、演奏モードからそれぞれのモードに入る入り口です。`REC` はサンプリングモード、`DEL` は DEL モード（音の削除）、`EDIT` は EDIT モード（編集）に入ります。録音するのは `REC` です。

## What the panel does when you enter Sampling mode
ja: ## サンプリングモードに入るとパネルはどうなる

type: quiz
id: q-3-14
cite: guide-book 29 "empty pads flash"
tags: sampling, intro

The moment you enter Sampling mode, what happens on the panel?
ja: サンプリングモードに入った瞬間、パネルでは何が起こりますか。

- [x] The `REC` button flashes and the empty pads flash
ja: - [x] `REC` ボタンが点滅し、空のパッドも点滅する
- [ ] The `REC` button lights steadily and every pad lights white
ja: - [ ] `REC` ボタンが点灯したままになり、すべてのパッドが白く点灯する
- [ ] All pads go dark until you select one
ja: - [ ] パッドを1つ選ぶまで、すべてのパッドが消灯する
- [ ] Only the display changes -- no lights change
ja: - [ ] 表示部が変わるだけで、ボタンやパッドの点灯状態は変わらない

### Why

The flashing is an invitation: the flashing `REC` button says the unit is armed but not
yet recording, and the flashing pads show you exactly which pads are free to record to.
ja: 点滅は合図です。点滅する `REC` ボタンは、録音待機中でまだ録音は始まっていないことを、点滅するパッドは、どのパッドに録音できるかを教えてくれます。

## What the Sampling mode display shows
ja: ## サンプリングモードの表示内容

type: quiz
id: q-3-15
cite: guide-book 29 "SAMPLING MODE: AUTO TRIGGER"
tags: sampling, intro

Describe what the display shows once you are in Sampling mode.
ja: サンプリングモードに入ると、表示部には何が表示されますか。

### Answer

It reads `SAMPLING MODE` with the current trigger setting (for example
`AUTO TRIGGER`), plus L and R level meters.
ja: `SAMPLING MODE` の文字と現在のトリガー設定（例：`AUTO TRIGGER`）、そして L と R のレベルメーターが表示されます。

### Why

Reading this screen is how you confirm which trigger will start the recording and
whether your input level is healthy -- both before you commit a sound to a pad.
ja: この画面を読めば、どのトリガーで録音が始まるか、入力レベルが適切かを、パッドに音を録音する前に確認できます。

## Changing the trigger setting
ja: ## トリガー設定の変更

type: quiz
id: q-3-16
cite: guide-book 29 "Select it with the directional buttons"
tags: sampling, intro

In Sampling mode, which controls select the trigger setting?
ja: サンプリングモードでトリガー設定を選ぶには、どれを使いますか。

- [x] The directional buttons `←`/`→`
ja: - [x] 十字ボタンの `←`/`→`
- [ ] The two dials
ja: - [ ] 2つのダイヤル
- [ ] The `A`-`D` buttons
ja: - [ ] `A`-`D` ボタン
- [ ] Tapping any pad
ja: - [ ] いずれかのパッドを叩く

### Why

The directional buttons `←`/`→` step through the trigger settings. The `A`-`D` buttons
choose banks, and tapping a pad in Sampling mode selects the recording target instead.
ja: 十字ボタンの `←`/`→` でトリガー設定を順に切り替えます。`A`-`D` ボタンはバンクの選択用で、サンプリングモードでパッドを叩くとサンプルを登録するパッドの選択になります。

## What AUTO TRIGGER does
ja: ## AUTO TRIGGER の働き

type: quiz
id: q-3-17
cite: guide-book 29 "starts recording automatically when sound is input"
tags: sampling, core

What does the `AUTO TRIGGER` setting do?
ja: `AUTO TRIGGER` はどのような設定ですか。

- [x] Starts recording automatically when sound is input
ja: - [x] 音を入力すると自動的に録音が始まる
- [ ] Starts recording the moment you select a pad
ja: - [ ] パッドを選んだ時点で録音が始まる
- [ ] Starts recording on the next beat of `MASTER BPM`
ja: - [ ] `MASTER BPM` の次の拍で録音が始まる
- [ ] Records only while you hold the pad down
ja: - [ ] パッドを押している間だけ録音される

### Why

With `AUTO TRIGGER`, the unit waits silently after you select a pad and starts
recording the instant sound arrives -- so you never record dead air at the front of a
sample. Starting on pad selection is the other trigger's job.
ja: `AUTO TRIGGER` では、パッドを選んだあと本機は静かに待機し、音が入力された瞬間に録音を始めます。サンプルの頭に無音を録ってしまう心配がありません。パッドを選んだ時点で録音が始まるのは、もう1つのトリガーの役割です。

## When MANUAL TRIGGER starts recording
ja: ## MANUAL TRIGGER で録音が始まるタイミング

type: quiz
id: q-3-18
cite: guide-book 29 "recording starts the moment you select a pad"
tags: sampling, core

With `MANUAL TRIGGER`, when does recording start -- and when would you choose it?
ja: `MANUAL TRIGGER` では、いつ録音が始まりますか。また、どんなときに選ぶとよいですか。

### Answer

Recording starts the moment you select a pad. Choose it when you want to decide the
sampling start timing yourself instead of letting the input sound trigger it.
ja: パッドを選んだ時点で録音が始まります。入力音まかせにせず、サンプリング開始のタイミングを自分で決めたいときに選びます。

### Why

`AUTO TRIGGER` waits for sound; `MANUAL TRIGGER` puts the start under your finger.
That control matters when the sound you are capturing has a quiet start that
`AUTO TRIGGER` might clip off, or when you want to start on a count.
ja: `AUTO TRIGGER` は音の入力を待ち、`MANUAL TRIGGER` は開始を自分の指にゆだねます。立ち上がりが静かで `AUTO TRIGGER` では頭が欠けてしまいそうな音を録るときや、カウントに合わせて始めたいときに役立ちます。
