# Derived extraction artifacts — regenerate with scripts/extract-pages.sh
manuals/pages/
manuals/text/
translations/work/

# Cross-agent dispatch artifacts (runner writes results + call log here)
state/

# Haskell build outputs
dist-newstyle/
*.o
*.hi
result

# Editor/OS noise
.DS_Store
*.swp

# M0 site build outputs — regenerate with scripts/build-site.sh; never committed,
# GitHub Actions uploads site/public/ as the Pages artifact instead.
site/public/
site/dist-newstyle/
